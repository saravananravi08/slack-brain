# T505 Work Log — Execute internal beta release

- **Task:** [`../tasks/T505-BETA-RELEASE.md`](../tasks/T505-BETA-RELEASE.md)
- **Status:** Planned
- **Owner:** Unassigned
- **Branch:** —

Append entries chronologically. Do not rewrite old entries. Never include secrets, private Slack text, database rows, or full traces.

## Entry format

```text
## YYYY-MM-DD HH:MM UTC — status/event
- Actor:
- Commit/worktree:
- Work performed:
- Files changed:
- Commands/checks:
- Result:
- Decisions/assumptions:
- Blockers:
- Next action:
```

## Entries

_No work recorded yet._


## 2026-08-30 22:10 UTC — beta preparation complete; execution blocked
- Actor: claude-planner-2 (task worker)
- Commit/worktree: `2dccf1f` in `../worktrees/T505`, branch `task/T505-execute-internal-beta-release`, branched from `integration/mastra-rewrite` @ `0839422`
- Work performed: Read the T505 spec, the merged T501 acceptance report, the T406 live-ingestion report, the T502 sign-off, and the T504 runbook. Verified P03/P04 code state directly rather than from the dashboard. Wrote `docs/releases/beta.md`: scope, pre-flight checklist, test-workspace deployment procedure, observation plan, exit criteria, blocker register, and an empty run record.
- Commands/checks: `npm run typecheck` clean; `npm test` — 41 files, 569 passing (4 skipped, 4 todo); `npm run build` **fails**; `npx tsc` + compiled start verified (fails closed on missing config); read-only `conversations.info` against the beta channel; secret scan of the new document clean; `git diff --check` clean.
- Result: Preparation complete. Beta not executed.
- Decisions/assumptions:
  - **The app is now in the beta channel.** `conversations.info` reports `is_member: true`. This is a change from T401's finding and is what makes a live beta feasible; it also means B-07 is resolved by simply running the beta, since the first human message is the evidence T406 needs.
  - **B-08 raised, and I recorded that I caused it.** `npm run build` fails because the F-05 fix removed the module-level `mastra` export. The security fix was correct — the import-time singleton read `process.env` and created a database directory before `parseConfig()` — but it broke the Mastra CLI. Impact on the beta is low because the documented start path is the compiled `dist/src/index.js`, not `mastra build`. The fix must not reintroduce the eager singleton, and it belongs to whoever holds `src/mastra/**` next. T501 had already found this; I confirmed it and attributed it.
  - **Historical recall is out of beta scope, and that has to be told to users.** B-03 means nothing has been imported, so Gist's corpus is only the beta window. "I couldn't verify that" will be its most common answer to historical questions — correct under D009, and indistinguishable from a defect to anyone who was not warned. Section 3 exists for that reason alone.
  - **T503 is the real dependency gap.** T505's "metrics meet launch thresholds" criterion cannot be evaluated because no thresholds exist. Recorded rather than quietly checked off.
  - **F-19's drop count is on the observation list.** The deferred fix turns on frequency, and the beta is the first chance to measure it.
- Blockers: B-08 (build); T503 unassigned; execution needs an operator; B-03 keeps historical recall out of scope.
- Next action: Coordinator to assign T503 and route B-08. Once an operator runs the window, results go into `docs/releases/beta.md` §10 and T406 collects its live evidence at the same time.
