# F19 Work Log — Ambient same-thread concurrency drop

- **Task:** [`../tasks/F19-CONCURRENCY-DROP-TEST.md`](../tasks/F19-CONCURRENCY-DROP-TEST.md)
- **Status:** Ready for Integration
- **Owner:** pi coding agent
- **Branch:** `fix/security-f19-test`

Never include secrets, private Slack text, databases, or full traces.

## Entries

## 2026-08-30 16:02 UTC — implemented and verified
- Actor: pi coding agent
- Commit/worktree: `fix/security-f19-test`; `/home/saravananravi/Documents/worktrees/F19`
- Work performed: Split the existing offline harness into synchronous dispatch and asynchronous drain phases. Added a real-adapter/real-Chat regression that dispatches a root at `T` and a reply with `thread_ts: T` before awaiting, holds root persistence open with a controlled promise, and pins the current same-thread drop.
- Files changed: `tests/integration/live-ingestion/slack-runtime.test.ts`; F19 task/log metadata.
- Commands/checks: `npm ci --ignore-scripts`; targeted Vitest; `npm run typecheck`; `npm test`; `git diff --check`; scope and forbidden-file review.
- Result: Targeted suite passed 15/15. Typecheck passed. Full suite passed 36 files with 2 skipped; 542 tests passed, 2 skipped, 4 todo. The second payload produced SDK `LOCK_FAILED`; persistence recorded only the root timestamp `T`.
- Failed attempt: Initial targeted run treated the expected raw SDK `LockError` as an unexpected drain failure. The test was tightened to expect code `LOCK_FAILED` while independently asserting the reply timestamp was absent; no harness-wide error suppression was added.
- Decisions/assumptions: Same channel is insufficient to prove contention. Root `ts: T` and reply `thread_ts: T` map to one Slack thread ID. This task measures the known defect only; moving ingestion off the concurrency-controlled path or counting drops remains a T502 design decision.
- Blockers: None.
- Next action: Integrator reviews and merges the regression; T502 retains the underlying design decision.
