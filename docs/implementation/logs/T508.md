# T508 Work Log — Remove legacy runtime after rollback window

- **Task:** [`../tasks/T508-LEGACY-CLEANUP.md`](../tasks/T508-LEGACY-CLEANUP.md)
- **Status:** Blocked
- **Owner:** pi coding agent (assessment only)
- **Branch:** `task/T508-remove-legacy-runtime-after-rollback-window`

Append entries chronologically. Do not rewrite old entries. Never include secrets, private Slack text, database rows, or full traces.

## Entry format

```text
## YYYY-MM-DD HH:MM UTC — status/event
- Actor:
- Commit/worktree:
- Work performed:
- Files changed:
- Commands/checks:
- Result:
- Decisions/assumptions:
- Blockers:
- Next action:
```

## Entries

## 2026-08-30 — legacy cleanup assessment completed; deletion blocked
- Actor: pi coding agent
- Commit/worktree: `task/T508-remove-legacy-runtime-after-rollback-window`; `/home/saravananravi/Documents/worktrees/T508`; base `956393c`
- Work performed: Assessed root legacy modules, active imports, package/lock state, environment/config surfaces, README/gitignore, deployment/rollback references, archive-schema references, and rollback-bundle requirements. No runtime file, dependency, config, or package manifest was changed or removed.
- Files changed: T508 task/log metadata only.
- Commands/checks: `npm ci`; static import/reference/env/dependency scans; candidate size/history comparison; `npm run typecheck`; `npm test`; `npm run build`; package-lock dependency inspection; `npx tsc --noUnusedLocals --noUnusedParameters`; Git status/ignore/history inspection.
- Result: Current Mastra baseline passes typecheck, 576 tests, and production build. Eight root modules are absent from all active imports/scripts, excluded from `tsconfig`, unchanged since initial commit `5068d09`, and technically dead in the Mastra runtime. T506 is still Planned and no rollback-window approval exists, so deletion is not authorized.
- Failed attempts: Initial direct `npx tsc --noEmit` ran before `npm ci` and failed because the worktree had no installed dependencies; the locked install followed and normal typecheck passed. One environment-key extraction used unsupported ripgrep lookaround; it was rerun with `grep`/`cut`.
- Decisions/assumptions: This branch is assessment/documentation only under direct assignment. Keep task Blocked; do not create an implementation/handoff commit claiming cleanup completion.
- Blockers: T506 completion, explicit rollback-window approval, verified immutable rollback bundle/database backup, and cross-scope contract/runbook updates described below.
- Next action: Coordinator assigns deletion only after all prerequisites in the recommended sequence are evidenced.

### Removal inventory after gate approval

| Candidate | Why it is dead | Planned removal | Main risk/prerequisite |
|---|---|---|---|
| `agent.ts` | Claude CLI child-process wrapper; no active import; request path uses Mastra OpenAI agent | Delete | Removes Claude fallback and embedded legacy persona/tool instructions; preserve verified rollback release first |
| `bot.ts` | Slack Bolt runtime; no active import/script; `@slack/bolt` already absent | Delete | Never delete/start while legacy rollback may be needed; prove legacy service stopped and one Socket Mode owner remains |
| `cron.ts` | Legacy Slack polling ingestion; no active script/import | Delete | Preserve source archive/database and confirm Mastra live ingestion/full import are authoritative |
| `db.ts` | `better-sqlite3` custom FTS/database layer; dependency already absent | Delete | `docs/architecture/contracts/archive-import.md` still calls its DDL authoritative; update that contract or pin immutable source reference first |
| `search.ts` | Shell search/summary/ClickUp CLI used only by legacy Claude prompts | Delete | Removes operator CLI; confirm nobody uses it outside repository and retain approved Mastra benchmark/import tooling |
| `clickup.ts` | Legacy ClickUp client used only by `search.ts`; ClickUp is excluded from rewritten product | Delete | Revoke legacy token after rollback closes; confirm no operator workflow depends on this unowned feature |
| `files.ts` | Temporary Slack download/extraction helper used only by old bot/cron | Delete | Securely inspect/purge host-side `files/` outside Git; do not mistake local downloaded content for rollback archive |
| `proactive.ts` | Legacy nudges, reactions, polls, greetings, digests; imported only by `bot.ts` | Delete | These capabilities are intentionally outside Mastra PRD; obtain product acknowledgement that no parity is expected |
| `README.md` | Entirely documents Claude CLI, Bolt-era scripts, FTS, ClickUp, nonexistent `deploy.sh`, and stale env keys | Replace with Mastra-only setup/runtime/import/test links | Do not preserve stale commands that could restart legacy or imply unsupported features |
| `@ai-sdk/anthropic` | Direct dependency has no source/test import; D007 was superseded by OpenAI D012 | Remove from manifest and regenerate lockfile | Do only in cleanup implementation; verify no approved rollback artifact depends on current manifest |
| `.gitignore` entry `files/` | Protects only legacy temporary downloads in repository code | Remove only after host/repo scan and secure purge | Keep if any approved local workflow still writes there; never commit recovered downloads |

All eight root modules total 3,397 lines / 121,538 bytes and form one closed legacy component graph. No active file under `src/`, `scripts/`, `tests/`, or `benchmarks/` imports them. Current package scripts contain no `bot`, `cron`, or `search` entry.

### Keep or handle separately

- Keep `.env.example`: it already contains only validated Mastra/OpenAI/Slack Socket Mode keys. No legacy key is present.
- Keep `.gitignore` database/WAL patterns, `.env*` protections, `.mastra/`, `dist/`, traces, logs, and `test_secrets.js`. Mastra still uses SQLite and secrets; these are not legacy-only ignores.
- Keep `dotenv` and `axios` lock entries: they are transitive dependencies of current Mastra CLI/core and Slack Web API, not direct legacy packages.
- Keep `@ai-sdk/openai`, `@chat-adapter/slack`, Mastra packages, `zod`, and all current scripts. `npm start` may not be the approved standalone Socket Mode command, but it is a current Mastra script—not legacy cleanup evidence.
- `src/mastra/channels/slack.ts` has one dead `SlackAdapter` import found by strict unused checking. It is outside T508 write scope; remove in a separate scoped fix. No other active unused import was reported.
- Historical PRD/migration/baseline references may remain as history, but live instructions and contracts must not point at deleted paths as current sources.
- Legacy environment names exist only in dead code/stale README: `SLACK_USER_TOKEN`, `SLACK_CHANNEL_ID`, `CLAUDE_MODEL`, `CLICKUP_API_TOKEN`, and `CLICKUP_TEAM_ID`; README additionally names `SLACK_SIGNING_SECRET`, `CLAUDE_API_KEY`, and `DATABASE_PATH`. Remove documentation/config copies and revoke corresponding secrets only after rollback closes.

### Rollback and documentation risks

1. **Current checkout is not a valid legacy rollback bundle.** Root files remain, but their packages and scripts were removed at T101. A rollback artifact must use a verified pre-rewrite release including its matching `package.json`, lockfile, service definition, credentials, and database. Initial commit `5068d09` contains the unchanged eight modules and legacy manifest, but Git history alone is not backup verification.
2. **Legacy data is not cleanup code.** `slack_messages.db`, source archive, pre-import/pre-cutover backups, and previous Mastra release must remain outside the active tree through the approved window and retention policy. Never delete them as part of repository cleanup without separate operator evidence.
3. **Schema reference would break.** `docs/architecture/contracts/archive-import.md` identifies `db.ts` as authoritative legacy DDL. Before deleting `db.ts`, copy the minimal schema into the contract or cite an immutable archived release; that path is outside T508 scope.
4. **Rollback/deployment docs still require legacy units.** `deploy/systemd/gist.service.in` has legacy unit conflicts and rollback/deployment runbooks describe starting old services. After approval, coordinator must update those cross-scope files to record retirement while preserving historical incident evidence.
5. **Capabilities disappear permanently.** Proactive posts, polls, scheduled digests, file extraction, ClickUp, shell search, and polling ingestion are not implemented by Mastra by design. Confirm product/operator acceptance before removing the last readable implementation.
6. **Secret deletion is revocation, not text cleanup.** Inventory and revoke old user, ClickUp, Claude, signing-secret, and retired Slack-app credentials in the approved secret store after rollback closes; never log values.

### Recommended execution sequence

1. Merge/complete T506; record production stability and explicit rollback-window approval.
2. Verify a restorable immutable legacy release plus matching lockfile/service config, legacy database/source archive, credentials, checksums, owner, and restore rehearsal outside Git.
3. Prove legacy bot/cron units are stopped/disabled and exactly one Mastra Socket Mode runtime owns events.
4. Coordinate required archive-contract, systemd, deployment, and rollback documentation updates outside T508 scope.
5. Delete the eight root modules together; rewrite README; remove `@ai-sdk/anthropic` and regenerate lockfile; conditionally remove only the `files/` ignore.
6. Add `tests/legacy/**` guards for forbidden active files/imports/scripts/config keys while allowing explicit historical documentation references.
7. Run task verification, full E2E/benchmark checks, secret/database scans, and rollback-asset verification before handoff.
