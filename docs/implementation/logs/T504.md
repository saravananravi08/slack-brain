# T504 Work Log — Write and rehearse deployment, backup, restore, rollback runbook

- **Task:** [`../tasks/T504-DEPLOYMENT-RUNBOOK.md`](../tasks/T504-DEPLOYMENT-RUNBOOK.md)
- **Status:** In Progress
- **Owner:** pi-coder-11
- **Branch:** `task/T504-write-and-rehearse-deployment-backup-restore-rollback-runbook`

Append entries chronologically. Do not rewrite old entries. Never include secrets, private Slack text, database rows, or full traces.

## Entry format

```text
## YYYY-MM-DD HH:MM UTC — status/event
- Actor:
- Commit/worktree:
- Work performed:
- Files changed:
- Commands/checks:
- Result:
- Decisions/assumptions:
- Blockers:
- Next action:
```

## Entries

## 2026-08-30 14:51 UTC — coordinator-approved early start
- Actor: pi-coder-11
- Commit/worktree: `task/T504-write-and-rehearse-deployment-backup-restore-rollback-runbook`; `/home/saravananravi/Documents/worktrees/T504`
- Work performed: Created isolated worktree from integration commit `67a782b`; read the task/workflow, accepted decisions, existing runbooks, configuration, storage, and runtime startup paths; confirmed dependency state.
- Files changed: `docs/implementation/tasks/T504-DEPLOYMENT-RUNBOOK.md`, `docs/implementation/logs/T504.md`
- Commands/checks: `git worktree add ../worktrees/T504 -b task/T504-write-and-rehearse-deployment-backup-restore-rollback-runbook integration/mastra-rewrite`; required file reads; dependency/status inspection.
- Result: T106 is merged/Completed. T307 and T406 are Planned and intentionally unresolved under coordinator-approved early start.
- Decisions/assumptions: Draft runbooks now; mark all T307 full-import evidence, T406 live-validation evidence, validated production start command, and rehearsals as pending. Keep task `In Progress`; do not mark `Ready for Integration` until dependencies merge and real evidence is incorporated.
- Blockers: Finalization intentionally pending T307 and T406; not a blocker to this draft-only commit.
- Next action: Verify build output/start mechanics, then draft deployment, backup/restore, and rollback procedures without private values.

## 2026-08-30 14:59 UTC — early draft verified
- Actor: pi-coder-11
- Commit/worktree: `task/T504-write-and-rehearse-deployment-backup-restore-rollback-runbook`; `/home/saravananravi/Documents/worktrees/T504`
- Work performed: Drafted deployment, backup/restore, and rollback runbooks plus a fail-closed systemd template. Covered secret/path checks, one Socket Mode owner, clean shutdown, D004 retention tiers, cold backups, restore purge requirements, previous-Mastra/legacy rollback, known offline-delete gap, escalation, and sanitized rehearsal templates.
- Files changed: `docs/runbooks/deployment.md`, `docs/runbooks/backup-restore.md`, `docs/runbooks/rollback.md`, `deploy/systemd/gist.service.in`, T504 task/log metadata.
- Commands/checks: `npm ci`; `npm run build`; build-output/startup inspection; `git diff --check`; required Slack-token scan; Bash fence syntax checks; local-link check; synthetic systemd-template rendering and verification; pending-marker inspection; `git status --short`.
- Result: Build passed. Token/diff/link/Bash checks passed. Rendered T504 unit has no template-owned systemd syntax warning. Initial unit check found `StartLimitIntervalSec` in the wrong section and was fixed. Two helper checks initially called unavailable `python`, then passed with `python3`.
- Decisions/assumptions: Generated Mastra server output does not include standalone `src/index.ts` Socket Mode startup, so `npm start`/generated server command is not claimed as production start. Template keeps `@T406_VALIDATED_START_COMMAND@` unresolved. Supported integrated message/vector retention and trace-prune operator commands are also pending rather than replaced by raw SQL.
- Blockers: Final T307 full-import/backup references, T406 live results/start/health commands, integrated retention/prune commands, host service names/paths/owners, and non-production restore/rollback rehearsal evidence remain pending real inputs.
- Next action: Commit this early draft with task status `In Progress`; keep branch open until T307/T406 merge and pending evidence can be rehearsed and incorporated.


## 2026-08-30 21:55 UTC — runbook brought current; F-12 constraint documented
- Actor: claude-planner-2 (task worker)
- Commit/worktree: `820a393` in `../worktrees/T504`
- Work performed: Merged `integration/mastra-rewrite` into the task branch first — the draft was 49 commits behind and described a runtime that no longer exists. Then: documented the F-12 single-instance constraint required by the T502 sign-off §3.1; added service and secret inventories; added health checks that work without T406; corrected the configuration checklist for D012.
- Commands/checks: `npm run typecheck` clean; `npm test` — 37 files, 550 passing (2 skipped, 4 todo); secret scan of `docs/runbooks` and `deploy` clean; `git diff --check` clean; scope diff within write scope.
- Result: Runbook content complete and current. Rehearsals still blocked.
- Decisions/assumptions:
  - **The start command was tested, not assumed.** `node --experimental-strip-types src/index.ts` fails with `ERR_MODULE_NOT_FOUND` because the sources use NodeNext `.js` specifiers — it must never go in a service unit, and the runbook now says so with the error text. `npx tsc && node dist/src/index.js` works and, with no configuration, prints variable names only and exits 1. The gate moved PENDING → PARTIAL on that evidence; Socket Mode hold/reconnect/SIGTERM remain T406's.
  - **Recorded a build caveat rather than fixing it out of scope:** `tsconfig.json` includes `tests/**`, so a plain `npx tsc` emits `dist/tests` next to `dist/src`. A build-time exclude belongs in a change that owns `tsconfig.json`.
  - **F-12 is written as a correctness constraint, not a capacity note.** The silent failure — an interleaved row/vector write leaving text and embedding disagreeing, with no error and no duplicate reply — is stated explicitly, because the loud failure (duplicate replies) is the one operators would otherwise assume is the whole risk. Cross-referenced from backup-restore.md and rollback.md, since a retention sweep or import run against a live service is the same unserialized second writer.
  - **Pending gates left open.** T307 and T406 are unmerged; nothing was marked rehearsed or cutover-approved.
- Blockers: rehearsals need a non-production environment and T307 (B-03) / T406 (B-07) evidence. Not resolvable from this branch.
- Next action: Coordinator may merge this as documentation. Rehearsal evidence and the final start command land after T307/T406.
