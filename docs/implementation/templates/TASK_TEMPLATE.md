# TXXX — Task title

- **Status:** Planned
- **Phase:** PXX
- **Owner:** Unassigned
- **Branch:** `task/TXXX-slug`
- **Parallel group:** PG-XX
- **Depends on:** —
- **Blocks:** —
- **Can run parallel with:** —
- **Conflicts with:** —
- **Write scope:**
  - `path/**`
- **Read-only references:**
  - `GIST_MASTRA_PRD.md`
- **Task log:** `../logs/TXXX.md`

## Objective

One measurable outcome.

## Deliverables

- Deliverable.

## Required procedure

1. Confirm dependencies are merged and task is assigned.
2. Create isolated branch/worktree from current integration branch.
3. Mark this task `In Progress`; start task log.
4. Implement only within write scope.
5. Add/update tests.
6. Run required verification and `git diff --check`.
7. Record results and decisions in task log.
8. Commit explicit paths with task ID in subject.
9. Record hash; mark `Ready for Integration`; commit handoff metadata.
10. Phase integrator merges, reruns checks, then marks `Completed` and updates phase/status/global log.

## Implementation steps

1. Step.

## Verification

```bash
# commands
```

## Acceptance criteria

- [ ] Criterion.

## Completion record

- Implementation commit: —
- Handoff commit: —
- Merge commit: —
- Integration metadata commit: —
- Completed at: —
