# F20 — Preserve live-message classification during persistence

- **Status:** Ready for Integration
- **Owner:** pi coding agent
- **Branch:** `fix/security-f20`
- **Source review:** [`../../security/design-review.md`](../../security/design-review.md)
- **Task log:** [`../logs/F20.md`](../logs/F20.md)
- **Write scope:**
  - `src/mastra/channels/**`
  - `src/ingestion/**`
  - `tests/**`
  - this task/log metadata

## Objective

Resolve security-review finding F-20 without relabelling subscribed-thread messages or dropping them as invalid persistence events.

## Completion

- [x] Live Slack projection preserves the normalizer's `ambient` or `addressed` classification and `addressed_to_gist` value.
- [x] Ambient persistence accepts consistent `ambient`/`false` and `addressed`/`true` inputs.
- [x] Regression coverage pins subscribed-thread forwarding and addressed persistence acceptance.
- [x] `npm run typecheck` passes.
- [x] `npm test` passes: 35 files / 538 tests.
- [x] Scope and whitespace checks pass.

## Commit record

- Implementation: this task's `F-20` commit on `fix/security-f20`.
